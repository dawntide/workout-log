/**
 * 세트 행 5열 그리드 — `[# | 중량 | 반복 | RPE | 완료]`.
 *
 * 행(`workout-set-row.tsx`)과 열 헤더(`workout-exercise-card.tsx`)가 같은 값을 써야
 * 칸이 어긋나지 않는다. 두 파일에 각각 선언돼 있던 것을 단일 소스로 모았다.
 */
export const SET_ROW_GRID =
  "var(--v2-s-6) minmax(0, 1fr) minmax(0, 1fr) minmax(0, 1fr) var(--v2-s-6)";
